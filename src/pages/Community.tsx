import { DojoHeader } from "@/components/DojoHeader";
import { useEffect, useState } from "react";
import { toast, Toaster } from "sonner";

const API = import.meta.env.VITE_API_URL || "https://data-dojo.onrender.com";
type User = { id: number; username: string; profile_picture?: string | null };
type Comment = { id: number; content: string; created_at: string; updated_at?: string; user?: User; likes_count: number; liked_by_me?: boolean; is_owner?: boolean };
type Post = { id: number; content: string; created_at: string; updated_at?: string; user?: User; likes_count: number; liked_by_me?: boolean; comments_count: number; comments?: Comment[]; is_owner?: boolean };

function avatarUrl(user?: User) { const value = user?.profile_picture; if (!value) return null; return value.startsWith("http") || value.startsWith("data:") ? value : `${API}${value.startsWith("/") ? value : `/${value}`}`; }
function Avatar({ user, size = "h-10 w-10" }: { user?: User; size?: string }) { const url = avatarUrl(user); return <div className={`${size} shrink-0 overflow-hidden rounded-full border border-orange-500/60 bg-zinc-800 flex items-center justify-center font-bold text-orange-400`}>{url ? <img src={url} alt={user?.username || "Usuário"} className="h-full w-full object-cover" /> : (user?.username || "S").slice(0, 2).toUpperCase()}</div>; }

export default function Community() {
  const [posts, setPosts] = useState<Post[]>([]); const [draft, setDraft] = useState(""); const [comment, setComment] = useState<Record<number,string>>({}); const [editingPost, setEditingPost] = useState<number|null>(null); const [editingComment, setEditingComment] = useState<number|null>(null); const [editText, setEditText] = useState(""); const [loading,setLoading]=useState(true); const token=typeof window!=="undefined"?localStorage.getItem("token"):null;
  const headers = { "Content-Type":"application/json", Authorization:`Token ${token}` };
  async function fetchPosts(){ if(!token){setLoading(false);return;} try{const r=await fetch(`${API}/api/community/posts/`,{headers:{Authorization:`Token ${token}`}}); if(!r.ok)throw new Error(); const d=await r.json(); setPosts(Array.isArray(d)?d:d.results||[]);}catch{toast.error("Não foi possível carregar a comunidade.");}finally{setLoading(false);} }
  useEffect(()=>{fetchPosts();},[]);
  async function publish(){if(!draft.trim()||!token)return; const f=new FormData();f.append("title","Post da Comunidade");f.append("content",draft.trim());const r=await fetch(`${API}/api/community/posts/`,{method:"POST",headers:{Authorization:`Token ${token}`},body:f});if(r.ok){setDraft("");toast.success("Postagem publicada no dojô 🥋");fetchPosts();}else toast.error("Erro ao publicar.");}
  async function likePost(id:number){const r=await fetch(`${API}/api/community/posts/${id}/like/`,{method:"POST",headers:{Authorization:`Token ${token}`}});if(r.ok){const d=await r.json();setPosts(p=>p.map(x=>x.id===id?{...x,likes_count:d.likes_count,liked_by_me:d.liked_by_me}:x));}}
  async function sendComment(id:number){const text=comment[id]?.trim();if(!text)return;const r=await fetch(`${API}/api/community/comments/`,{method:"POST",headers,body:JSON.stringify({topic:id,content:text})});if(r.ok){setComment(c=>({...c,[id]:""}));fetchPosts();}else toast.error("Erro ao comentar.");}
  async function likeComment(id:number){const r=await fetch(`${API}/api/community/comments/${id}/like/`,{method:"POST",headers:{Authorization:`Token ${token}`}});if(r.ok)fetchPosts();}
  async function savePost(id:number){const r=await fetch(`${API}/api/community/posts/${id}/`,{method:"PATCH",headers,body:JSON.stringify({content:editText})});if(r.ok){setEditingPost(null);fetchPosts();toast.success("Publicação editada.");}else toast.error("Não foi possível editar.");}
  async function saveComment(id:number){const r=await fetch(`${API}/api/community/comments/${id}/`,{method:"PATCH",headers,body:JSON.stringify({content:editText})});if(r.ok){setEditingComment(null);fetchPosts();toast.success("Comentário editado.");}else toast.error("Não foi possível editar.");}
  return <div className="min-h-screen bg-background text-foreground"><Toaster position="top-right" theme="dark" richColors/><DojoHeader/><main className="mx-auto max-w-5xl px-4 py-10"><div><div className="text-xs uppercase tracking-[.25em] text-muted-foreground">Comunidade</div><h1 className="font-display font-extrabold text-4xl mt-1">Salão dos Samurais</h1><p className="text-muted-foreground mt-1">Conquistas, dúvidas e insights de quem trilha o caminho dos dados.</p></div><section className="mt-8 rounded-xl border border-border bg-card p-5"><textarea value={draft} onChange={e=>setDraft(e.target.value)} placeholder="Compartilhe sua conquista, dúvida ou insight Kaizen..." className="w-full min-h-28 rounded-lg border border-border bg-background p-3 text-sm text-white"/><div className="mt-3 flex justify-end"><button onClick={publish} className="rounded-md bg-destructive px-5 py-2 font-bold">Publicar</button></div></section><div className="mt-6 space-y-5">{loading?<p>Carregando comunidade...</p>:posts.map(post=><article key={post.id} className="rounded-xl border border-border bg-card p-5"><header className="flex items-center justify-between"><div className="flex items-center gap-3"><Avatar user={post.user} size="h-12 w-12"/><div><div className="font-semibold">{post.user?.username||"Samurai"}</div><div className="text-xs text-muted-foreground">{new Date(post.created_at).toLocaleString("pt-BR")}{post.updated_at&&post.updated_at!==post.created_at?" · editado":""}</div></div></div>{post.is_owner&&<button onClick={()=>{setEditingPost(post.id);setEditText(post.content)}} className="text-xs text-orange-400">Editar</button>}</header>{editingPost===post.id?<div className="mt-4"><textarea value={editText} onChange={e=>setEditText(e.target.value)} className="w-full rounded-md bg-background p-3"/><button onClick={()=>savePost(post.id)} className="mt-2 text-orange-400">Salvar</button></div>:<p className="mt-5 whitespace-pre-wrap leading-7">{post.content}</p>}<div className="mt-5 flex gap-5 text-sm"><button onClick={()=>likePost(post.id)} className={post.liked_by_me?"text-orange-400":"text-muted-foreground"}>❤️ {post.likes_count}</button><span>💬 {post.comments_count}</span></div><div className="mt-5 border-t border-border pt-4"><div className="flex gap-2"><input value={comment[post.id]||""} onChange={e=>setComment(c=>({...c,[post.id]:e.target.value}))} placeholder="Escreva uma resposta..." className="flex-1 rounded-md border border-border bg-background px-3 py-2"/><button onClick={()=>sendComment(post.id)} className="text-orange-400">Responder</button></div><div className="mt-4 space-y-3">{(post.comments||[]).map(item=><div key={item.id} className="flex gap-3 rounded-lg bg-background/60 p-3"><Avatar user={item.user} size="h-9 w-9"/><div className="flex-1"><div className="flex justify-between"><div className="text-sm font-semibold">{item.user?.username||"Samurai"}</div>{item.is_owner&&<button onClick={()=>{setEditingComment(item.id);setEditText(item.content)}} className="text-xs text-orange-400">Editar</button>}</div>{editingComment===item.id?<div><input value={editText} onChange={e=>setEditText(e.target.value)} className="mt-1 w-full bg-card p-2"/><button onClick={()=>saveComment(item.id)} className="mt-1 text-xs text-orange-400">Salvar</button></div>:<div className="mt-1 text-sm text-zinc-300 whitespace-pre-wrap">{item.content}</div>}<button onClick={()=>likeComment(item.id)} className={`mt-2 text-xs ${item.liked_by_me?"text-orange-400":"text-muted-foreground"}`}>❤️ {item.likes_count}</button></div></div>)}</div></div></article>)}</div></main></div>;
}
